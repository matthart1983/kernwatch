# Requirement completion ledger

> Historical ledger: the [12 September tab/table audit](TAB_GAP_AUDIT.md) identifies additional correctness and implementation gaps, including screens marked validated below. Use the [current completion plan](TAB_IMPLEMENTATION_PLAN.md) for outstanding work.

Acceptance audit against the original specification, 11 September 2026. `validated` means implementation and recorded checks on the declared environment, not certification of every kernel, driver or terminal. Optional enrichment and hardware boundaries are stated in each row. The specification remains the original acceptance target; reference corrections are documented in VISUAL_REVIEW.md.

A follow-up [gap audit](GAPS.md) adds metadata collectors and corrects over-broad completion claims. Rows marked partial still contain implementation work.

Evidence: [tests](VALIDATION.md), [Clippy](VALIDATION.md), [PTY tour](VALIDATION.md), [live timing](VALIDATION.md), [renderer benchmark](VALIDATION.md), [VM](VALIDATION.md), [second kernel](VALIDATION.md).

## Screens

| Requirement | Owner / change | Visual capture | Interaction / evidence | Live source | Status | Remaining blocker / boundary |
|---|---|---|---|---|---|---|
| SCREEN-00 | `ui/views.rs` — Eight panels, braille CPU split, core strips, concerns and shared timeline | [dense](../screenshots/current/00-dense.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | validated | Some detailed metadata is available by drill rather than in the compact Dense panel |
| SCREEN-01 | `src` — Unified Issue projection drives hero/health severity and finding prose | [overview](../screenshots/current/01-overview.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | validated | None |
| SCREEN-02 | `src` — Thread identity, grouping, policy/nice/age, placement, runtime/wakeup histories, watch/copy and scoped capture | [tasks](../screenshots/current/02-tasks.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | Scheduler weight is collected; affinity-origin attribution still needs an event source; thread memory is not summed as separate address spaces |
| SCREEN-03 | `src` — CPU/task/cgroup selectors, wake/runtime/migrations, p50/p99, top task, row histories, scale/histogram toggle and off-CPU capture | [scheduler](../screenshots/current/03-scheduler.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | validated | Bounded distribution estimates; scheduler internals are not inferred from kernel version |
| SCREEN-04 | `src` — Six cards, slab/NUMA/hugepages, allocation/reclaim, full process memory columns, unit toggle and slab/process/syscall tour | [memory](../screenshots/current/04-memory.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | PSS is slow and limited to 32 leaders; optional allocation attribution requires additional evidence |
| SCREEN-05 | `inventory.rs; probes.rs` — Ten device columns, queue/controller candidates, completed versus pending latency | [block](../screenshots/current/05-block.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | validated | Controller MSI candidates are not an assertion of exact driver queue-to-vector mapping; SMART optional |
| SCREEN-06 | `src` — Shared syscall columns, negative returns/errno, mean/p99/max, caller/verdict/history, latency/error filters, full arguments/stack inspection | [syscalls](../screenshots/current/06-syscalls.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | x86-64 bounded decoder; return codes and addresses may need further interpretation/symbolization |
| SCREEN-07 | `src` — Independent IRQ worker, configured/effective/observed placement, interval change events, row histories and selected IRQ/CPU plot | [irq](../screenshots/current/07-irq.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | Exact change actor and driver queue-to-vector causality are not inferred from polling |
| SCREEN-08 | `src` — Eleven columns plus CPU history, scoped limits/PSI, quota overlay, pressure toggle, unit fragment and reviewed changes | [cgroups](../screenshots/current/08-cgroups.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | validated | Manager properties and drop-ins are collected where the bus is accessible; fallback scope and errors are explicit |
| SCREEN-09 | `src` — Nine columns, parameter/detail inspection, timestamped baseline acceptance, analyst mark, module lifecycle events and historical taint | [modules](../screenshots/current/09-modules.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | On-disk signer/vermagic implemented; loader and hook attribution remain incomplete; loaded sysfs state stays distinct |
| SCREEN-10 | `src` — Program inventory, owned ID/attachment attribution, verdict/history, maps, owner overhead and bounded capture controls | [ebpf](../screenshots/current/10-ebpf.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | Creator UID, verifier count, link targets and static helper targets implemented; FD-holder attribution, map occupancy and runtime distributions remain incomplete |
| SCREEN-11 | `src` — Bounded distinct records, follow/filter, source/time navigation, diagnosis correlation, event rate and actual capture health | [dmesg](../screenshots/current/11-dmesg.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | partial | Kernel/audit journal fallback implemented; interactive tracefs fallback and cross-suspend log alignment remain incomplete |
| SCREEN-12 | `src` — Issue/evidence navigation at captured times, proposed live previews, simulation, readback, measured verification and shared report | [diagnose](../screenshots/current/12-diagnose.png) | Layout buffers, breakpoints and workflow/PTY tests | procfs/sysfs or compiled trace backend | validated | Causal claims remain hypotheses until discriminating evidence is captured |

## Shared requirements

| Requirement | Owner / implementation | Visual / interaction / live evidence | Status | Remaining blocker / boundary |
|---|---|---|---|---|
| VIS-01 | `ui/views.rs` — Dedicated layouts and numbered panels | Linked captures, workflow tests and validation reports above | validated | See screen-specific scope and optional enrichment above |
| VIS-02 | `ui/widgets.rs` — Calibrated semantic palette, terminal fallback | Linked captures, workflow tests and validation reports above | validated | No physical-terminal colour calibration claim |
| VIS-03 | `ui/widgets.rs` — Braille at full and inline sizes; bounded bucket histograms | Linked captures, workflow tests and validation reports above | validated | Fixed fixture scales; histogram values are estimates |
| VIS-04 | `src` — Bounded/virtualized tables, dedicated content allocation and full-screen focus | Linked captures, workflow tests and validation reports above | validated | Sparse sources retain empty space instead of inventing rows |
| VIS-05 | `ui/views.rs` — Seven terminal sizes and each compact focused panel | Linked captures, workflow tests and validation reports above | validated | Reference grids documented in SPEC |
| VIS-06 | `ui/widgets.rs` — Explicit rectangles, display-width ellipsis, wrapped details and cause nodes | Linked captures, workflow tests and validation reports above | validated | Full values in inspector; terminal font-dependent raster differences |
| NAV-01 | `app.rs; settings.rs` — Stable shortcuts, Dense default and persisted view | Linked captures, workflow tests and validation reports above | validated | None |
| NAV-02 | `ui.rs` — Separate record/freeze/replay/history and trace status | Linked captures, workflow tests and validation reports above | validated | Abbreviated inactive tabs at constrained width |
| NAV-03 | `app.rs` — Focus, route restoration, named sorting and palette | Linked captures, workflow tests and validation reports above | validated | Source/time drill tours are covered by workflow tests |
| NAV-04 | `src` — Boot/task start identities and device/cgroup/module/program generations, IRQ names, source routes and retained entity frames | Linked captures, workflow tests and validation reports above | validated | Changes that occur entirely between polls cannot be reconstructed |
| NAV-05 | `src` — Contextual footers, scope/metric/scale/unit controls, named sort palette and action review | Linked captures, workflow tests and validation reports above | validated | Unavailable sources include concrete acquisition reasons |
| DATA-01 | `domain.rs` — Typed measurements, intervals and acquisition quality | Linked captures, workflow tests and validation reports above | validated | Some legacy formatted view rows remain beside typed entities |
| DATA-02 | `src` — Task start/exit, actual CPU IDs, device sysfs identity, cgroup inode, module sysfs/source version and BPF tag/load identity | Linked captures, workflow tests and validation reports above | validated | Polling cannot discover a disappear/reappear cycle entirely between samples |
| DATA-03 | `domain.rs; app.rs` — Monotonic capture, wall mapping, bounded shared cursor and explicit gaps | Linked captures, workflow tests and validation reports above | validated | Entity retention may be shorter than graph retention under memory cap |
| DATA-04 | `enrich.rs; fixture.rs` — CPU normalization and PSI semantics | Linked captures, workflow tests and validation reports above | validated | No addition of overlapping worker/vector execution |
| DATA-05 | `inventory.rs; probes.rs` — Runtime quota, scoped distributions, completed/pending separation | Linked captures, workflow tests and validation reports above | validated | Bounded sample estimates; direct cgroup membership at switch-in |
| DATA-06 | `src` — One fixture clock, reconciled CPU/group/runtime/device histories, pre/incident/quota/recovery, time-bounded evidence and explicit simulation | Linked captures, workflow tests and validation reports above | validated | Historical distributions not retained by the fixture are unavailable rather than copied from the future |
| COL-01 | `collect.rs; enrich.rs; inventory.rs; sources.rs` — Independent bounded topology workers, live Linux sources and source quality | Linked captures, workflow tests and validation reports above | validated | PSS limited to 32 process leaders per slow cycle |
| COL-02 | `probes.rs; tracing.rs` — Wake/switch/migrate/exit, CPU/task/cgroup and off-CPU correlation | Linked captures, workflow tests and validation reports above | validated | Declared kernel/BTF support and explicit loss invalidate confidence |
| COL-03 | `probes/kernwatch.bpf.c; probes.rs` — IRQ/block/syscall event lifecycle and bounded decode/stack data | Linked captures, workflow tests and validation reports above | validated | Kernel-specific attach; exact driver merges need broader stress coverage |
| COL-04 | `bpf.rs; probes.rs` — Compiled CO-RE objects, inventory/stats, bounded ring and worker aggregation | Linked captures, workflow tests and validation reports above | validated | High-rate raw-event ingest can overflow; loss is surfaced and metrics degraded |
| COL-05 | `main.rs` — Unprivileged, explicit tracing and offline replay | Linked captures, workflow tests and validation reports above | validated | x86-64 kernels tested in VM; other architectures not supported |
| REC-01 | `recording.rs; app.rs` — Versioned framing, replay speed/seek and independent recording/freeze | Linked captures, workflow tests and validation reports above | validated | 512 MiB encoded replay limit |
| REC-02 | `recording.rs` — Eight actual files with manifest sizes, environment and action journal | Linked captures, workflow tests and validation reports above | validated | Report publishes atomically; a crash may leave hidden staging data; errors are surfaced |
| DIAG-01 | `diagnose.rs` — Persistent scoped baselines, readiness and hysteresis | Linked captures, workflow tests and validation reports above | validated | Causal attribution remains explicit inference |
| DIAG-02 | `src` — CPU/scheduler/I/O/pressure/slab/module/quota/overhead and interval-based IRQ-placement rule families | Linked captures, workflow tests and validation reports above | validated | The placement rule explicitly labels correlation and preserves causal uncertainty |
| ACT-01 | `actions.rs; app.rs` — Concrete no-write review, exact ID confirmation, pre/post verification | Linked captures, workflow tests and validation reports above | validated | Proposed supported live targets open a concrete preview; applying requires the reviewed ID |
| ACT-02 | `actions.rs` — IRQ/RPS/task/cgroup controls with intent journal/readback/revert | Linked captures, workflow tests and validation reports above | validated | Task and cgroup real writes tested in VM; real IRQ/RPS hardware writes not exercised |
| QA-01 | `src` — Thirteen reviewed actual-buffer captures, symbol/foreground goldens and documented diagram corrections | Linked captures, workflow tests and validation reports above | validated | Terminal fonts and raster antialiasing differ; no pixel-identity claim |
| QA-02 | `src` — Dense→scheduler→task→cgroup, slab→process→syscall, block→IRQ→CPU, log→diagnosis→preview→verification→report tours | Linked captures, workflow tests and validation reports above | validated | Privileged execution is tested separately in the disposable VM |
| QA-03 | `tests; tests/vm` — Unit/workflow tests and two-kernel disposable VM acquisition/action checks | Linked captures, workflow tests and validation reports above | validated | Broader hardware and lifecycle coverage remains useful |
| QA-04 | `scripts/pty_performance.py; examples/benchmark.rs` — 10k tasks/256 CPU/1k group rendering, live PTY, CPU/RSS and overload | Linked captures, workflow tests and validation reports above | validated | Host/workload-specific evidence; physical display paint excluded |
| QA-05 | `docs/COMPLETION.md` — Independent requirement status and explicit gaps | Linked captures, workflow tests and validation reports above | partial | Optional metadata and tested hardware boundaries remain explicit; validation is scoped to the recorded environments |
